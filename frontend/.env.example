# Development (Vite dev server, proxied to http://localhost:5000)
# VITE_API_URL=http://localhost:5000/api

# Production (served through Nginx, which proxies /api to the backend container)
VITE_API_URL=/api
